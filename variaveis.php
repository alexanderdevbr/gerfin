<?
# Variaveis GLOBAIS utilizadas no sistema

# Menu Principal Superior
$menu_cadastros = isset($_GET["menu_cadastros"]) ? $_GET["menu_cadastros"] : '';
$menu_cartoes   = isset($_GET["menu_cartoes"]) ? $_GET["menu_cartoes"] : '';
$menu_pagaberto = isset($_GET["menu_pagaberto"]) ? $_GET["menu_pagaberto"] : '';
$menu_sql       = isset($_GET["menu_sql"]) ? $_GET["menu_sql"] : '';
$menu_logout    = isset($_GET["menu_logout"]) ? $_GET["menu_logout"] : '';
?>